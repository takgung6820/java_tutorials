package list.linkedlist.implementation;

import javafx.scene.Node;

public class LinkedList {
    // 첫 번째 노드를 가리키는 필드
    private Node head;
    private Node tail;
    private int size = 0;
    private class node {
        // 데이터가 저장될 필드
        public Object data;
        // 다음 노드를 가리키는 필드
    }
}
